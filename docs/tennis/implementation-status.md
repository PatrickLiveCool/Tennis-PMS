# Tennis PMS 实施状态

更新：2026-09-19。来源 Green PMS v1.4.3 / 47eb658a20aee5fc469a6ecbb17444999385da6a。

**本地 MVP 已进入综合验收：网球运营页面、可信登录、多租户、交易及外部助手适配均已落地。支付/退款仍为明确标记的本地模拟，外部 Runtime 与微信渠道没有接入；不代表真实渠道或客户人工验收完成。**

## F10 异常实收原路退款（2026-09-19）

- 订单取消/到期后的迟到付款、同单额外付款及充值额外实收，可以由有权限的员工核对原金额、填写原因后全额原路退款；不改变正常订单、库存或钱包本金/赠送。
- 金额和原渠道交易号取已验证实收登记；每条异常唯一内部退款，固定原商户快照。未知查询原号；明确失败才可重试新渠道代次，旧失败不覆盖新代次。
- 只有已验证退款成功才在同一事务关闭异常，并写入负向现金台账及受权限过滤的退款结果事件；没有手工标记已退款入口。普通与异常退款共享渠道退款号防重登记。
- 020 为追加迁移，独立开发库和测试库均已应用。最终 typecheck、完整单元 69 文件 / 1,284 项、PostgreSQL 集成 19 文件 / 242 项、网球 build、PR 格式 8 项通过；后端及 HTTP 定点 28 项也已通过。
- 浏览器完成合成订单 `d4427d89` 的 120 元迟到实收核对：退款失败仍 OPEN，重试保留内部退款 `2776554b`、使用新渠道代次，成功后 RESOLVED；台账负向现金 120 元、余额/赠送均为零，刷新后可从流水重开原退款。最终构建 `tennis-zkaJ2uJj.js` 已加载。
- 独立只读代码审阅未发现确定的资金/权限问题；此结论不代替运行验证或人工业务验收。截图仍有工具视口裁剪，未声称全响应式验收完成。

契约见 [异常实收退款](cash-exceptions.md)。仍有 [已核实本地工作流缺口](remaining-local-acceptance.md) 继续实施；真实微信、商户、外部 Runtime、生产环境与客户人工验收保持待完成。

## F9 支付适配与恢复（2026-09-19）

- 通用支付端口与持久 Mock 渠道记录，统一订单/充值/改期/退款的创建、查单与已验签结果。实际外部渠道仍未接入。
- 平台维护租户商户版本，新付款保存不可变快照；换版和停用不改变旧款查单/退款的商户。平台界面只接收凭据引用，WECHAT 预配置明确未接通。
- 同事务保存业务意图和渠道任务；领取短租约后事务外请求。UNKNOWN 按原号查单；可信观察可重放，资金入账保持现有幂等与钱包比例规则。
- 仅确认终态失败的退款可由授权员工重试，新增渠道代次、保留原内部退款与资金分配。旧失败不覆盖新代次，通知签名绑定操作编号。
- 订单付款、充值、退款记录展示本地快照、渠道状态及查询原结果入口。保留 GreenPMS 视觉与原模拟能力，不生成虚构二维码。
- 018/019 为追加迁移；开发库与测试库均已应用，旧迁移校验未改。
- 最终 typecheck、69 文件 / 1,284 项单元、18 文件 / 226 项 PostgreSQL 集成、网球 build、PR 格式 8 项全部通过。数据库用例包含新支付恢复 15 项和商户绑定 16 项，最终全量运行一次通过。
- 浏览器加载 `tennis-BziE9V6x.js`，合成订单 `e4205cfa` 按原操作查询，模拟到账后订单和渠道同为成功；平台保存第二合成租户 MOCK 商户第 1 版。界面沿用 GreenPMS，未发生真实扣费。截图有现有工具视口裁剪，未据此声称所有断点均已验收。

契约见 [支付渠道与商户绑定](payment-channel.md)。真实微信、商户与外部 Runtime 联调、真实资料导入和客户人工验收仍未完成。

## F8 外部接入基础（2026-09-19）

- 平台可创建/撤销单租户 Gateway 凭据，租户管理员人工绑定已有客户/员工主体；后台新增渠道接入与身份管理。凭据仅创建时显示一次，不保存在浏览器持久缓存。
- 入站消息去重、有限授权重放、回复完成与人工接管使用持久请求标识。重复授权只恢复原 token/原期限；解绑/停用与接管后旧身份在实际事务内失效。排队消息快照固定到目标消息，不混入未来指令。
- 外部工具可查询同租户完整时段、多片需求的替代场馆，复用当前价格；推荐不占场，换场馆须新会话授权和报价。
- 同事务业务事件、随机游标续读及按客户/员工权限过滤；长期 Gateway 读凭据可在单次写授权结束后核对。
- 请求与命令回执同事务关联，后台办理记录和 Gateway 可定位已提交业务。未知结果不自动重派，也不恢复旧写 token。
- 015–017 为追加迁移，原001–014未改。开发库/测试库已应用。
- 自动验证当前已完成：68 文件 / 1,269 项单元；15 文件 / 185 项 PostgreSQL 集成（含 Gateway 13、发现12、事件10）。新增恢复定点测试曾因测试把 `order.id` 误写为 `order.orderId` 失败，修正测试后 Gateway13项及全量185项均通过；日志保留。
- 补充请求关联测试9项通过，合计16文件/194项数据库用例；最终typecheck、网球build及PR格式8项通过。最终Web资产为 `tennis-C0PYmzgZ.js`。
- 浏览器完成租户管理员绑定/撤销合成员工身份；助手客户会话显示“结果待核对”及原订单 `3a2c1310` 的真实当前状态。界面沿用原视觉；未触发真实微信或付款。

F8 当时剩余的支付通用端口和持久化恢复已由 F9 本地实现。真实微信/商户/Runtime/生产环境和业务人工验收未完成；Goal 保持 active。协议分别见 [Gateway](gateway.md)、[业务事件](business-events.md)、[请求恢复](external-agent.md) 与 [替代场馆](agent-discovery.md)。

## F7 运营补齐（2026-09-19）

- 订单目录改为服务端搜索、状态/场馆当地日期筛选和游标翻页；较早创建的今日预约不再受最新 100 单限制。今日仅显示当日有效明细，已取消明细排除，跨日组合单逐明细展示。
- 钱包完整历史可翻页，员工/客户和外部工具遵守原权限；SQL 原时间精度与稳定游标避免同刻流水、新充值造成跳项或重复。界面每页 50 条，保留查询位置。
- 授权员工可按明细取消/调整未付款组合单，保留原付款期限、保留原因和未选明细；失败保留原库存。待处理付款或实收未核对前拒绝调整。新增 014 迁移区分未付款改期及取消前未付款的历史行，后续付款/退款不会重新计入被取消金额。
- 本轮验证在相同源码的既有本地副本完成：typecheck、67 文件 / 1,255 项纯单元、网球 build 通过。真实 PG 首轮 145 项通过，新增目录测试 5 项因合成记录缺必填字段失败；修正仅测试 fixture 后定点 5 项通过，合计覆盖 12 文件 / 150 项。原失败日志与补验日志均保留，不把首轮写为全通过。
- 浏览器：51 条模拟钱包流水按 50+1 翻页并返回；关键词查到订单 `5c0d5849`；未付款双片单取消第二片，待付 240→120；保留第一片后改至第三片，待付 120→80，原截止时间/原因保留，无付款和退款记录。真实资金未动。
- 发现并固化 [外部接入缺口](external-integration-gaps.md)：后台助手适配尚不能代表微信 Gateway 完整接入；当时列出同租户替代场馆工具、可信 Gateway 身份/入站、业务事件、未知请求关联及支付适配端口等本地工作；前四项已由 F8 补齐，支付端口仍待后续实施。

本轮日志：总项目 `outputs/Tennis-PMS-F7-verification.log`；正式文件与验证副本按明确清单核对。当前本地服务由验证副本提供，以避开再次出现的 iCloud dataless 读取等待；正式交付目录仍为 Tennis PMS。Goal 保持 active。

## F6 已有实现

- 可信账号/会话、CSRF 与工作区版本；平台、员工、客户身份分离，租户/场馆/操作权限由服务端复核。平台开通租户及首位管理员，可停用/恢复租户服务；停用不删预约和资金，撤销外部授权，到期任务仍运行。
- 复用 GreenPMS 样式、表单、弹窗、深绿导航。独立网球排场、订单、客户余额、工作台、资产/营业/定价/折扣/充值方案/员工设置页面；15 分钟调度，常用 1 小时，同单多片及不同时间明细。
- 充值、本金/赠送、余额预留和混合付款、部分取消/退款、按明细改期补差、多原付款退款；补款成功才原子换场，失败或过期保留原预约。价格与可退资金上限分别展示，资金台账不重复计算充值和储值消费。
- 保留 AI 助手、页面/订单上下文、可编辑常用提问、已解决/未解决反馈和人工接管。外部工具包含最小选客、排场、报价/预订、付款与余额查询；没有内建 Agent Runtime。平台统一配置模型/Base URL/外部服务，租户不可配置；密钥不回填。
- 009–013 为追加迁移：登录会话、助手/授权、改期及退款组合、外部派发去重、按主体隔离的助手反馈。旧迁移未改。助手接管/租户停用后旧 token 失效，未知外部执行结果必须核对后恢复。
- 未决交易键与草稿按主体/身份/租户/客户/场馆隔离；切回同一身份可恢复。2xx 响应截断仍视为结果未知，重试被拒绝也不丢失首次未知记录；成功清理在弹窗卸载前持久化。

## F6 自动验证与浏览器证据

- 完整纯单元：67 文件 / 1,232 项通过。
- 正式仓库最终真实 PostgreSQL 10 文件 / 127 项全部通过；正式仓库 typecheck、67 文件 / 1,232 项纯单元与 build 均通过。来源版本一致性、diff 空白检查及 PR 格式 8 项也已有通过记录。早期测试字段错误及云盘读取中止记录保留在收尾日志，不冒充通过。
- 浏览器实际完成：同段两片 160 元订单，余额 100 + 模拟渠道 60；取消一片退 80（余额 50 / 渠道 30）；另一片升价改期补 40 后换场，再降价退 40 并按两笔原付款拆退；连续改期旧草稿已修复并复验。
- 浏览器实际完成：100 元无赠送充值及余额更新；保存分时折扣并拒绝同片同时重叠；未配置外部 AI 时人工接管与留言；客户本人订单/余额与受限导航；平台租户列表/统一 AI 设置；手机底栏交互。
- 手机视口工具截图存在缩放与裁剪偏差，DOM 尺寸及底栏交互已核对；真实手机触屏、中文输入法和客户人工验收保持待验收。助手真实回复/反馈只用测试 transport 的集成用例验证，不冒充真实模型联调。

日志：[正式仓库最终验证](</Users/feather/Documents/Codex project/Tennis/outputs/Tennis-PMS-F6-formal-repository-verification.log>)；[F6 收尾验证](</Users/feather/Documents/Codex project/Tennis/outputs/Tennis-PMS-F6-final-verification.log>)。

## 当前交付及外部待办

- 正式代码目录 `/Users/feather/Documents/Codex project/Tennis PMS`，分支 `feat/tennis-foundation`。临时验证副本用于避开 iCloud 读取卡顿，按明确文件列表同步并逐字节核对。
- F6 正式仓库构建成功；F7 Web 4273 / API 4200 从已核对的验证副本运行，健康检查返回 Tennis PMS / MOCK。独立 PostgreSQL 55439。演示账号、两个租户及全部交易均为合成数据；随机凭据仅保存在本地忽略目录。
- 真实微信入口、商户支付/退款、外部 Runtime、指定模型实际联调、真实资产/价目/有效占用/期初导入、客户人工验收和生产部署仍待条件具备。
- GitHub 远端是公开仓库。本阶段先交付本地可审阅结果，没有公开推送客户访谈与需求资料，也没有合并或上线。
- Goal 保持 active，缺少外部条件只暂停依赖部分，不能把本地结果标成完整项目上线完成。

## 以下为历史阶段记录

以下日期段落保留当时实现及验证口径，其中“尚未完成”“下一步”仅说明该阶段历史，不替代上方当前状态。

## F5 线上充值及渠道交易号去重（2026-09-19）

- 008 追加迁移提供租户充值档位、5 分钟充值报价、唯一充值付款单、渠道实收总登记、充值事件回执及额外实收退款待办。任意正整数分充值默认无赠送，赠送来自租户管理员配置的档位；客户/智能体不能从请求中自行增加赠送。
- 确认充值保存本金/赠送快照；后续修改或停用档位不追溯改已确认单。重复确认、同报价换键、重复已验签回调均只入账一次。付款必须是本金金额，赠送不作为渠道实收。
- 充值没有场地库存，因此不套用 10 分钟锁场超时；渠道先失败后查到有效成功仍按原单入账，同单额外实收产生待办，不重复充余额。订场与充值共用渠道交易号登记，防止一笔现金被用于两个用途。
- 补强员工/客户双重身份下的回执隔离：客户上下文只能看到属于该客户的操作结果，不能凭该主体过去的员工操作键读取其他客户业务标识。
- 全量检查在匹配源码临时副本/Node 22 完成：typecheck；单元 65 文件/1,225 项；真实 PG 集成 5 文件/63 项（新增线上充值 5 项、双身份回执 1 项）；build；PR 格式 8 项均通过。38 个网球源码/测试文件逐字节一致，独立 dev/test 已迁移至 008。
- 日志：[F5 验证](</Users/feather/Documents/Codex project/Tennis/outputs/Tennis-PMS-F5-online-topups-verification.log>)。目前所有外部支付/退款仍为本地模拟事件，未发起真实渠道交易。现有 Web 构建仍是住房界面；公开认证/API、网球 UI、改期差额、真实联调及人工验收未完成。goal 保持 active，代码未提交/推送。

## F4 储值、混合付款与订单退款（2026-09-18）

- 006/007 追加迁移建立钱包账户、充值批次、本金/赠送、可用/预留额度、资金流水、付款尝试、验签事件回执、异常退款待办及按明细退款记录。005 及更早迁移未改；dev/test 均已应用至 007。
- 授权员工凭收款凭据登记线下充值/赠送，同凭据不重复入账；客户仅查自己绑定账户，员工需会员管理权限。按原充值次序及比例分配扣款，退回原批次和构成；同租户跨校区使用、不同租户隔离。
- 纯余额付款原子完成扣款与订单确认；混合付款先预留余额、仅申请差额，收到已验签且商户/币种/金额/单号匹配的模拟到账后才消费。并发不足余额拒绝；未付款取消、到期、付款失败释放预留，付款失败同时释放场地。
- 重复到账不重扣；同渠道交易号不能支付两单。到期/取消后的迟到实收及同单额外实收进入明确退款待办，不抢回已释放的库存。此待办尚未接入 UI/外部退款执行器。
- 授权员工确认原因和逐明细退款金额，可部分取消、零退款取消或保留使用权退款。总额和逐明细均限制累计可退金额；本金/赠送及外部来源分摊守恒。纯余额退款立即原路恢复；有外部部分时保留待处理状态，模拟渠道确认后整体完成。失败保留原分配可重试，不腾出额度让另一命令重复退钱。
- MVP 先按租户串行化订单/资金命令，账户另有行锁，避免跨校区到期和付款的锁顺序反转；不同租户并行。此为明确吞吐取舍，不宣称高并发容量已验收。
- 全量检查在匹配源码临时副本/Node 22 完成：typecheck；单元 65 文件/1,225 项（网球 45 项）；真实 PG 集成 5 文件/57 项（新增资金/退款 16 项）；build；PR 格式 8 项全部通过。网球源码/测试共 35 文件逐字节核对一致。
- 日志：[F4 验证](</Users/feather/Documents/Codex project/Tennis/outputs/Tennis-PMS-F4-wallet-refunds-verification.log>)。build 仍为住房 Web，不是网球界面验收。支付类显式为 LocalMockPaymentGateway，仅接受本地私有密钥签名事件，不提供真实微信商户实现，真实接入未完成。

F4 当时的后续范围中，线上充值已由 F5 补齐内部服务；接下来仍按完整 MVP 推进按明细改期及增减费用、认证和租户/场馆管理 API、运营排场/订单/客户/资金界面、平台配置的外部助手适配、受控命令/回执/事件与人工接管，以及模拟演示数据、浏览器验证、PR 和人工验收。暂无商户号/服务器/域名/真实价目资料，相关真实联调与导入保持外部待完成，goal 继续 active。

## F3 报价、组合订单及到期释放（2026-09-18）

- 005 迁移建立租户内客户档案、正式报价、订单/明细和操作回执。5 分钟报价保存服务器价格，确认时复查配置/权限/客户和库存；有效报价不随调价追溯更改。
- 一单同场馆多片、不同时间明细原子占用；重复提交和同报价换键恢复同单，异意图重用键拒绝。10 分钟普通待付款占位、授权员工注明理由和截止时间的保留预约、零价无需支付状态各自明确。
- 查询/重试返回当前订单状态；未付款取消、到期释放及审计原子执行，停用租户也可被系统任务释放到期占位。生命周期入口待接入服务进程定时任务。后台单条占用接口不能绕过订单释放/改期。
- 客户仅访问可信绑定身份下自己的记录；员工按场馆/动作权限代办。身份认证边界仍待开发，不能将客户端自报 actor 作为登录身份。
- 临时副本 Node 22：typecheck、完整单元 64 文件/1,222 项、真实 PG 集成 4 文件/41 项、build、PR 格式 8 项全部通过；新增预订 16 项。正式源码已与测试副本逐文件核对一致；005 已应用独立 dev/test。构建仍为住房 UI。
- 日志：[F3 验证](</Users/feather/Documents/Codex project/Tennis/outputs/Tennis-PMS-F3-booking-verification.log>)。钱包/混合支付/已付款退改下一步按 [资金契约](wallet-payments.md) 实施，尚无真实支付或人工验收完成声明。

## F0 基线：已实现

- 固化总项目 MVP、PMS 组件边界、待定业务规则与分阶段验收口径。
- 独立本地 Compose、55439 端口、tennis_dev / tennis_test 数据库与 tennis schema；独立迁移历史和 checksum。
- 停用根 npm 的住房运行、导入、重置型测试和发布入口；原生产 Compose 与发布/回滚/清理工作流移至非活动目录。远端尚未推送，GitHub 现有状态未改变。
- 分钟级 [startAt,endAt) 区间、空闲窗口计算、租场/课程/维护占用、数据库并发互斥、版本检查、改期失败回滚、释放保留历史。
- 内部库存函数没有公开 API；重复 ID 拒绝写入，尚未实现完整命令幂等、授权和审计。

## F0 基线：已验证

- 最终在正式 Tennis PMS 目录用 Node.js 22.23.2 复验全部下列检查，通过。独立 Node.js 22 位于本仓库忽略目录 `.local-workspace/node22`，未修改全局 Node。前期 Node.js 24.14.0 检查也通过。
- npm run typecheck：通过（包含网球脚本与配置）。
- npm test：63 个文件、1,199 项通过；其中网球区间及环境守卫为 19 项。其余是住房纯单测回归，不是网球功能验收。
- npm run build：通过，构建仍为继承的住房 Web；不能当作网球排场界面。
- node --test scripts/check-pr-tests.mjs：8 项通过。
- docker compose config：通过；独立 PostgreSQL 16 容器健康；开发库网球迁移成功。
- npm run test:integration：8 项真实 PostgreSQL 测试通过，包含同时抢场、未提交事务冲突等待、相邻/45 分钟场次、不同场地、课程/维护互斥、改期失败保留原记录、版本冲突、释放历史与直接 SQL 约束。
- 迁移首次执行及重复执行通过；集成测试只清理本次合成记录，不重置数据库/schema。

调试时修正了网球 CLI 的 ESM 入口，以及并发测试中事务内统计快照导致的等待状态观测问题；最终上述检查通过。

## 尚未完成 / 待人工验收

- 公开认证及实际网球业务 API、价格、待付款占位/到期任务、订单生命周期、人民币余额/混合支付、业务命令回执和运营页面；F1 已补内部员工库存授权及审计。
- 外部客户/员工智能体接口与后台助手适配、人工接管、真实支付/退款。智能体及 Runtime 由 PMS 体外提供，本次不开发；暂无商户号、服务器、域名及真实价目。
- 人工业务验收、远端 CI、PR/合并与网球生产配置。
- API 4200 与 Web 4273 仅为预留，无服务监听；数据库保留供后续开发。
- 集中确认已完成，具体占位、退改、未付款预约、人民币余额/混合支付等见 MVP 和决策清单；实际外部接入条件仍待具备。

## 本地交付位置

- 代码目录：`/Users/feather/Documents/Codex project/Tennis PMS`。
- 分支：`feat/tennis-foundation`；改动留在工作区，未提交、未推送。
- 验证日志：[Tennis-PMS-F0-verification.log](</Users/feather/Documents/Codex project/Tennis/outputs/Tennis-PMS-F0-verification.log>)。
- 网球 PostgreSQL 容器正在运行，挂载已切换到正式目录。Green PMS 工作区仍干净。

## 需求补充：同人同段多片（2026-09-18）

已确认该场景属于 MVP，撤回“一笔预约仅一片场地”的假设，已更新规格。F0 已验证不同球场的时段可并存；没有实现整组占位事务或预订订单/明细，既有测试数量不代表新增场景通过。用户已确认校区／场馆属于同一层 Venue，下面包含多片 Court；现有 venue_id 只是文本字段，正式场馆目录仍待实现。本次修改文档，没有改迁移、数据或业务代码。

## 需求补充：15 分钟粒度、常用 1 小时、不同时间明细（2026-09-18）

最新设计要求为最小调度粒度 15 分钟、常用预订时长 1 小时；常用时长不等同于最短可售时长。同一笔预订的各条明细可有不同球场和不同起止，同片不连续明细之间的空档不占用。当前代码只校验整分钟，预订单/明细聚合与整组事务也未实现；本次同步文档和验收目标，未改代码、迁移或数据库，未复跑业务测试。此前验证结果仅证明原 F0 基线。

## 需求补充：SaaS、多租户及租户自助配置（2026-09-18）

用户已同意多租户 MVP、格林首个试点、第二测试租户验证隔离、我方人工开通和收取软件服务费。租户后台维护资产、营业／维护状态、标准小时价格和分时折扣纳入范围。当前没有开通真实租户，也未实现租户隔离、正式场馆目录、定价后台或折扣引擎；本次仅同步规格和验收目标，未修改代码或数据库，没有新增测试通过的声明。

## 需求补充：GreenPMS 体验延续与 AI 助手（2026-09-18）

用户明确要求延续原系统视觉与操作习惯，保留后台 AI 助手。已核对 v1.4.3 源码及 2026-09-06 历史截图，建立 [体验延续清单](experience-continuity.md)，同步 MVP 和开发约束；历史截图的导航及 AI 入口不代表 v1.4.3 完整界面。用户同时确认同球场同使用时间最多一条有效时段折扣、重叠解决后方能生效；会员／券叠加等仍待定。

本轮仅修改文档，检查本地链接、开发快照一致性及差异空白格式。未修改 UI、业务代码或数据库，未复跑业务测试或启动新版页面。原 UI/AI 源码仍保留，网球及多租户适配尚未实现，不构成网球界面人工验收通过。

## 最新集中答复与持续开发 goal（2026-09-18）

用户已确认：会员以人民币金额管理，不额外打会员折扣；同租户跨校区通用，本金/赠送分别记账、按充值先后和比例扣款，退款恢复原构成；允许余额＋微信补差但禁止透支。预订、占位、退改、员工待收款例外及课程/财务范围已一次确认，详见 [决策与验收清单](decisions-and-acceptance.md)。现有范围、体验与决策清单共同作为轻量 PRD，不另做重复访谈。

AI 模型和 Base URL 仅由我方平台统一配置；智能体/Runtime 在 PMS 外部，本次保留后台助手 UI 和外部服务适配、受控业务 API。暂无商户号、测试服务器/域名、真实价目，使用模拟数据本地开发，真实接入/期初核对保持未完成。goal 已建立并保持 active，尚未达到完整 MVP 交付条件。

## F1 已实现与验证（2026-09-18）

- 领域层与追加 002 迁移落实 15 分钟边界，支持 45 分钟及常用 1 小时；不合规时间拒绝，不静默取整。001 内容/checksum 未变，迁移器校验连续历史并顺序执行追加迁移。
- 追加 003 建立 Tenant、Venue、员工主体/成员权限、授权场馆、球场与占用租户归属及复合外键。没有自动开通真实租户；旧无归属球场如存在会拒绝升级，不擅自分配。
- 查询、创建、改期和释放改为必须提供可信员工主体＋租户；事务内验证成员/租户状态、场馆范围和动作权限，业务写入同时留审计。公开登录/API 尚未提供，不能将调用方自报身份当作认证。
- 两个合成租户隔离、同名球场、只读角色、受限场馆、停用/撤权、跨租户引用拒绝、原/目标场馆改期授权及失败保原记录均有真实 PG 验证。
- 在非云盘临时副本 `/private/tmp/tennis-pms-goal-build` 使用 Node 22.23.2：typecheck 通过；完整单元 63 文件/1,209 项通过（网球 29 项）；网球真实 PG 集成 2 文件/17 项通过；build 及 PR 格式 8 项通过。构建仍是住房 UI，不是网球页面验收。
- 正式代码逐文件与验证内容核对一致；独立 tennis_dev 已应用 001–003，tennis_test 同步到 003；仅清理本次合成记录，无 DROP、无住房库操作。
- 正式目录检查因 iCloud 占位读取长时间等待被中止，没有把它记为通过。依赖按原锁文件离线恢复，旧依赖目录保留；从 Git index 恢复 387 个未修改跟踪源文件，现有工作区改动未覆盖。临时副本用于匹配源码验证，正式交付仓库仍是 Tennis PMS。
- 验证日志：[F1 15 分钟](</Users/feather/Documents/Codex project/Tennis/outputs/Tennis-PMS-F1-time-grid-local-verification.log>)；[F1 租户](</Users/feather/Documents/Codex project/Tennis/outputs/Tennis-PMS-F1-tenant-verification.log>)。

下一步：资产/营业配置与价目、确定性报价、多明细订单/占位、人民币余额/混合支付、运营 UI 与外部智能体接口。当前 F1 是受控库存基础，不是完整多租户 SaaS 或客户人工验收完成。

## F2 资产/营业/定价与空闲查询（2026-09-18）

已实现内部服务，尚未接入网球 UI 和公开认证/API：

- 场馆/球场创建及维护、营业时间与最短可售时长、独立的价格配置权限、小时单价及带日期/星期/钟点/球场范围的折扣。新资产未配置完整前不可售；名称可重复，ID/租户/场馆归属不可借编辑转移。
- 分段确定性计价，按场馆 IANA 时区匹配星期与日期，跨午夜和夏令时使用实际时长；整数分与 BigInt 中间计算，逐明细舍入并按最大余数分配显示段，避免合计不一致。会员不另打折。
- 重叠折扣保存拒绝，同场馆配置写入串行化，两个并发重叠配置只能成功一个；停用规则再启用仍需通过校验。修改保留审计与配置版本。
- 自动从营业窗口生成可用区间，扣除租场/课程/维护；完整时段匹配多片，不能拼接不同片的局部空闲来虚报数量。关闭/缩短营业规则影响未来有效预约时拒绝并列出相关记录，不静默释放。
- 当前 `priceSelection` 是价格估算，未持久化正式的 5 分钟报价，也未建立订单；不代表已锁场或旧订单价格快照已实现。下一切片契约见 [报价与交易](booking-transactions.md)。

验证：临时副本 Node 22 下 typecheck 通过；完整单元 64 文件/1,222 项通过（新增定价 13 项）；真实 PG 集成 3 文件/25 项通过（新增资产/定价 8 项）；build 及 PR 格式 8 项通过。测试覆盖跨折扣 90 元示例、日期/星期/时区/夏令时、舍入守恒、未配置不可售、价格权限、跨租户、并发折扣、整段多片匹配及受影响预约保护。正式仓库源码与测试副本逐文件一致。004 已应用至独立 tennis_dev/tennis_test，旧迁移未改。

日志：[F2 验证](</Users/feather/Documents/Codex project/Tennis/outputs/Tennis-PMS-F2-catalog-verification.log>)。构建仍是继承住房界面；网球 UI、完整报价/订单/支付/余额和外部接口仍待完成，无真实商户联调或人工业务验收完成声明。

## F11 新客户接入、人工协作与充值恢复（2026-09-19）

本轮已补齐本地运营流程并完成全量自动验证。以下为新增记录，前文各阶段的历史状态及验证范围保留；本轮限定浏览器证据见下文。

- 租户 ADMIN 可选择尚无登录主体的已有客户档案，首次渠道绑定在同一事务内创建内部 subject 并关联 Gateway。不会创建密码账号、登录会话、员工授权或平台角色，也不按同名或手机号自动合并。并发首次绑定只保留一个客户主体；冲突完整回滚，解绑、接入/客户/租户停用继续在实际业务事务复查。
- 后台助手、Gateway 入站及人工接管持久保存已验证的 page/orderId。客户会话即使由员工回复，也只能关联该客户在当前场馆的订单；跨租户、跨场馆或其他客户订单拒绝。消息重复提交必须保持正文与 context 一致，不能删除或替换原上下文；旧 Runtime 请求和 Gateway grant 始终锚定原消息快照。
- 人工会话详情提供最近关联订单，关联记录不受最近 200 条消息窗口限制。会话目录支持具体姓名/编号检索、HUMAN/AGENT 筛选与分页；原数组接口兼容。分页保留 PostgreSQL 微秒边界，并绑定当前身份、场馆与查询条件，每页重新授权。
- 会员页增加来自数据库的线上充值目录，可找回当前客户、场馆的待付款/待核对、失败及成功记录，不依赖设备草稿。打开原记录复用渠道查询与恢复入口；未知结果继续核对原单，查询目录不创建商户、渠道任务或钱包记录。`topup.begin` 回执恢复可按原 topupId 打开付款记录。
- AI 助手及 GreenPMS 的面板、筛选、状态标签、错误恢复和表单习惯继续保留；关联业务记录通过原详情入口重新鉴权。本轮浏览器已核对新客候选与模拟绑定、旧 HUMAN 会话可达与关联订单、持久充值记录恢复，以及排场刷新后的冲突与草稿保留；未覆盖全部手机断点、真实触屏和丢响应故障注入。
- 021 为消息上下文字段、订单引用外键和会话目录索引；022 仅增加充值目录索引，不改本金/赠送、充值生命周期或旧迁移内容。

本轮自动验证在 `/private/tmp/tennis-pms-goal-build` 完成，结果如下：

| 检查 | 结果与范围 |
| --- | --- |
| `npm run typecheck` | 通过 |
| 完整单元测试 | **69 文件 / 1,284 项通过**；包含继承系统回归，不将总数等同于网球独立功能数 |
| PostgreSQL 集成测试 | **22 文件 / 276 项通过**；包含新客户绑定 13 项、订单上下文/会话目录 8 项、充值目录 9 项及 Gateway/真实 HTTP 权限边界回归 |
| 网球 Web 构建 | 通过；使用 `apps/web/vite.tennis.config.ts`，生成 `dist-tennis`，本轮产物 `tennis-DdXUaxTr.js` |
| PR 格式检查 | 8 项通过 |

数据库及 HTTP 验证涵盖新客户首次绑定与并发回滚、旧绑定撤销后不继承会话、HUMAN context 持久化、同消息改 context 拒绝、旧消息授权快照、超过 100 条会话/充值的可达性、微秒时间分页、客户/场馆/租户隔离、撤权及目录无资金副作用。F11 A、Gateway context 和充值目录另经独立只读代码审阅，未发现确定的权限或并发缺陷；该审阅不代替运行与业务验收。

原始验证日志：[typecheck](/private/tmp/tennis-f11-typecheck.log)、[单元](/private/tmp/tennis-f11-unit.log)、[PostgreSQL 集成](/private/tmp/tennis-f11-integration.log)、[网球构建](/private/tmp/tennis-f11-build.log)、[PR 格式](/private/tmp/tennis-f11-pr-check.log)。实施契约见 [人工协作](operator-completion.md) 与 [持久充值目录](topup-directory.md)。

真实微信/Gateway 渠道身份核验与实际消息收发、各租户商户的真实支付/退款、指定模型/Base URL 与外部 Runtime 联调、服务器/域名与生产部署、真实业务资料导入及客户人工验收仍未完成。当前结论是本地实现与自动验证通过，不代表真实渠道、生产上线或客户人工验收完成。


### F11 限定浏览器核对

在本地 MOCK 模式下由 Codex 浏览器实际操作：

- 会话目录首页 20 条后加载更多，可找回第 22 条 HUMAN 会话 a6a8421a；人工筛选及搜索关联订单 e4205cfa 均能定位该会话。打开已付款 120 元的关联订单后关闭，原助手会话保留。
- 没有当前充值草稿时，从会员线上充值历史打开 23 元待付款原单 4d354dab，模拟成功后仍是原充值/渠道操作；余额从 11,826.67 增为 11,849.67 元，赠送额不变。
- 新建且无 subject 的合成客户 ef4668b7 在渠道身份候选中可选，管理员填写本地核验依据后提交，页面显示 f11-synthetic-new-customer → F11 新球友（合成）启用绑定；不关联真实微信。
- 2026-09-22 1 号场 18:00–19:00 取得 120 元报价后，以本地服务创建合成维护冲突 db726181；刷新显示“该时段已被占用”，旧报价撤回，客户和所选明细保留，报价按钮禁用。合成占用保留作验收证据。

以上为开发者浏览器核对，不代替客户人工验收。完整浏览器丢响应故障注入、长页滚动恢复的跨设备检查、真实手机触屏及全断点外观仍待人工验收。后端未知结果/幂等与权限已有自动测试，前端未决消息快照和恢复路径经代码审阅，二者分别记录。

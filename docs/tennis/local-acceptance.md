# 本地演示与验收记录

## F18 实际验证记录（2026-09-20）

- 全量单元：74 文件、1394 项通过；类型检查、构建、PR 格式 8 项通过。
- PostgreSQL 首轮 25 文件共 318 项：313 通过、5 失败。修复测试对数据库时间的过窄假设及超时后的清理竞态后，4 文件定点复验 51/53 通过，剩余两项超时；独立复验其所属 2 文件，19/19 通过（退款场景 3.4 秒，205 条历史分页场景 8.5 秒）。新增后台助手 12 项、企微对照 15 项通过。没有声称首轮全绿，也没有修改产品业务期限以适应测试。
- 024/025 迁移首次、重复执行通过，旧业务摘要保持不变。首次失败留下的两个精确合成测试租户已核对并清理。
- 浏览器：员工 AI 助手与业务会话分离；80 元合成订单确定引用自动归账；17 元合成充值无引用人工核对后归账；订单为 CONFIRMED/PAID，充值 SUCCEEDED。自动刷新回调已修复；最终重新加载页面状态一致，未再次执行新成交证明无需刷新。
- 平台管理员页面已核对模型、Base URL、密钥、启停与测试连接入口。未配置密钥，未发送真实模型请求。窄屏检查实际 CSS 宽 468px，页面 scrollWidth 同为 468px；截图工具有裁切，不代表完整真机视觉验收。临时视口已恢复。
- 当前构建资产 tennis-DvxLJvXz.js / tennis-ChHBwP5C.css。本地 http://127.0.0.1:4273/，支付模式 MOCK。
- 真实模型传输被自动审批拒绝，默认 connectionAvailable:false。需明确模型服务目的地及允许发送的数据范围后接线。真实企微同步、退款 adapter 和外部 Runtime 未接通；无部署、真实扣费或远端发布。客户人工验收尚未签收。


更新：2026-09-20。本页区分待执行的 F18 验收步骤与此前已完成的开发者验证；客户人工验收尚未签收。当前仅本地合成收款，真实模型传输、企微收退款同步及外部 Runtime 尚未接通。

## 启动和账号

按 [本地开发](development.md) 启动独立数据库、模拟数据、API 与 Web，打开 `http://127.0.0.1:4273`。

| 演示账号 | 身份 |
| --- | --- |
| demo.platform | 平台运营方，管理租户、统一 AI 设置和租户 Gateway 接入 |
| demo.green | 格林模拟租户管理员 |
| demo.staff | 同租户前台员工，按授权办理 |
| demo.customer | 同租户演示客户，仅本人订单和余额 |
| demo.second | 第二模拟租户管理员，用于隔离验证 |

随机密码保存在仓库忽略目录 `.local-workspace/demo-credentials.json`。不得复制住房凭据；不要把本地模拟配置直接用于生产。

## F18 本轮人工验收步骤（待执行）

本节是验收清单，不是通过记录。F18 自动验证、迁移、构建及开发者浏览器结果见本页 F18 实际验证记录。先确认本地页面显示“本地模拟”，使用合成客户与未来空闲场地；不要复制 GreenPMS 的真实业务数据或凭据。

| 步骤 | 操作 | 应看到的结果 |
| --- | --- | --- |
| 1 平台配置 | 用 `demo.platform` 进入“后台 AI 助手配置”；在空白演示配置填写占位模型、`https://model.example.invalid/v1` 及仅本地演示用占位 Key，保存。记录并恢复原启停状态；已有非演示配置时不覆盖 | 有模型、Base URL、密钥保留/替换/清除、启停与测试入口；没有外部 Runtime URL。保存后只显示“已保存密钥”，不回显原 Key |
| 2 区分保存和连接 | 保存后使用“测试模型连接”；切换租户身份查看后台助手状态 | 默认 `connectionAvailable:false`，明确提示真实连接未启用；测试不能显示成功，不发真实请求。配置信息可以保存，不意味着已能得到模型回答 |
| 3 双入口与私有会话 | 用 `demo.green` 打开“AI 助手”，新建本人会话、点击常用问题填入文本、关闭后重开；从订单详情“询问此订单”打开；再进入“业务会话” | 后台助手保留草稿及当前订单提示，关闭后返回原订单。业务会话仍能查看客户/员工协作、办理记录及授权人工接管；两个入口不混用会话和模型设置 |
| 4 租户与身份隔离 | 在两个模拟租户、不同场馆及 `demo.staff` / `demo.customer` 身份间切换，返回各自页面 | 不出现另一租户的流水或助手草稿；员工个人助手会话不向同租户其他员工开放。租户无平台模型设置/密钥权限；企微全租户归属确认仅租户管理员可见和执行 |
| 5 确定引用自动归单 | 新建一片一小时待付款订单，发起 MOCK 付款但不要使用原有“模拟成功”操作；需要混合付款时先核对合成钱包余额。在“工作台→企微收款对照”选择对应业务，生成“带明确业务引用”的合成流水 | 合成实收金额等于该次外部支付金额；系统自动关联并更新订单。混合付款只扣一次预留余额，现金与余额分别展示；无需再点逐笔“确认归属” |
| 6 无引用人工例外 | 为另一笔待付款业务生成“无业务引用”合成流水；查看订单后，在该流水上点“核对归属”，选择原业务，填写本地核验依据并确认 | 生成时停留待确认归属，订单不会仅因金额相同自动成交。核对区显示完整流水、商户、时间和实际金额；确认后只登记一次，金额/商户不符不能提交 |
| 7 筛选与历史业务 | 核对一笔未匹配流水时切换筛选、查询或翻页；再用保存的原渠道操作编号查找已完成业务 | 非待恢复选择应清空，不能把隐藏的旧流水归到当前业务；历史显示原支付金额及状态，不误写为当前欠款。准确查询历史不是再次收款 |
| 8 迟到实收 | 先为合成待付款订单创建支付意图并保存原渠道操作编号；待占位自然到期、刷新释放后，在收款对照中用原编号查找并生成确定引用流水 | 显示实收异常，原订单不恢复占场。若该时段已被另一测试预约占用，不影响新预约；按流水提示的关联场馆进入“实收待核对”处理 |
| 9 异常退款与台账 | 在对应场馆打开合成异常，核对原额、填写原因，按现有模拟渠道验证成功/失败及原操作恢复 | 只有可信模拟退款成功才关闭异常并出现负向现金流水；不改正常订单、已释放库存或钱包本金/赠送。未匹配收款不应冒充已完成订单收入 |
| 10 充值与多明细回归 | 新建合成充值支付意图，通过确定引用收款验证一次入账；再走同段多片、不同时间组合预约，以及既有部分改退和余额明细流程 | 充值本金/赠送各自留痕，合成实收不包含赠送；订单/充值不能争用同一流水。保留一位预订人整单付款，不出现多人 AA 或平台分账功能 |
| 11 操作习惯 | 桌面和实际手机检查排场、工作台、模型设置、后台助手及业务会话，切页后返回原位置；输入中文、换行、关闭弹窗再返回 | GreenPMS 导航、颜色、信息密度和操作习惯延续；无外层横向溢出，输入及确认控件可达，不因输入法确认字误发送 |

发生提交结果未知时保留页面提示的原操作，不新建相同付款。收款对照中的“核对并恢复原操作”沿用原业务、流水或合成命令 ID；刷新只读查询。需要验证“后台已提交但响应丢失”时，由开发者在本地受控注入一次响应中断，记录恢复前后唯一流水/资金事实；不要将普通刷新代替该故障验证。

后台助手的真实回答、工具调用质量和模型连接成功暂不可在默认 demo 验收。权限、消息幂等、失败恢复、只读工具和反馈由注入合成 transport 的自动用例验证，运行结果见本页 F18 实际验证记录；后续接通真实模型后，须另验实际回答和服务可用性，不能把本节配置保存算作完成。

验收记录应保存：日期、账号身份、场馆、操作步骤、订单/流水编号、预期与实际结果及是否为合成数据。未发生真实扣费、真实微信回复或生产部署。

## F18 之前已完成的浏览器业务验证

| 操作 | 实际结果 |
| --- | --- |
| 一人同段订两片：4/3号场 20:00–21:00 | 订单46e7c038共160元，整组预订成功 |
| 余额100＋模拟渠道60 | 渠道成功前保留待处理，成功后订单确认，资金来源分别留痕 |
| 取消4号场，退80 | 仅该明细取消，3号场保留；退款拆为余额50/原渠道30 |
| 3号场80改至1号场120 | 补40前原场保留，补款成功才改到1号场 |
| 连续改回3号场80，人工核准退40 | 按两笔原付款拆26.67与13.33；最终余额退16.67、原渠道退23.33，旧改期草稿不再复活 |
| 自定义充值100 | 不赠送；模拟成功后总余额从11726.67变为11826.67，明细本金增加100 |
| 分时折扣 | 新规则可保存，重叠规则提示中文冲突并拒绝写入 |
| AI尚未接入 | 明确显示未配置，人工接管及工作人员留言可用 |
| 客户登录 | 只显示客户业务入口，可核对本人订单/余额，无资产配置或人工退款入口 |
| 平台登录 | 可看两租户及统一AI配置，不默认进入租户业务；第二模拟租户停用/恢复均已在正式本地实例完成 |
| 订单助手 | 传入订单46e7c038上下文；快捷提问仅填入不发送，关闭后保留原订单详情 |
| 手机断点 | 桌面侧栏隐藏，底栏可进入本人余额，页面外层无显著横向溢出 |

浏览器使用稳定网球构建及本地合成数据，以上模拟付款/退款没有真实资金动作。原生日期输入的自动填值未触发浏览器受控事件时，改用实际键盘输入核验；没有把失败尝试记成产品通过。手机截图工具的缩放/裁剪存在偏差，真实手机触屏及中文输入法需客户验收。

## F8 页面验证

稳定构建 `tennis-C0PYmzgZ.js` 已刷新加载，使用合成集成“F8 渠道恢复演示（模拟）”。租户管理员从已有员工列表选择“演示前台”，填写人工核验依据后绑定 `f8-browser-synthetic-staff`，再填写原因撤销；页面状态分别更新为已绑定、已撤销。

同一后台助手选择客户会话 `f3e4bad5`，在“办理记录”中读取请求 `c1a06ded` 的 UNCERTAIN 回报与已提交 `quote.confirm`，定位唯一原订单 `3a2c1310`。核对时显示待付款/未付款；该模拟单仍按原10分钟期限到期，不因接管恢复库存或写授权。此验证没有调用真实 Runtime、发送微信或发生真实资金动作。

平台接入创建/撤销服务及权限由 PostgreSQL 测试覆盖；本轮浏览器验证集中于租户身份管理和请求恢复。平台凭据创建页面仍需客户实际操作验收。

## 自动覆盖

F7 已补充实际页面验证：合成客户“F7 历史查询与未付款调整（模拟）”的 51 条流水按 50+1 翻页、上一页恢复；搜索 `5c0d5849`，原双片未付款 240 元，取消第二片后 120 元，第一片调整到第三片后 80 元；保留原截止时间 09/19 04:54（Asia/Shanghai）和保留原因，付款/退款记录仍为空。该订单依旧会按原规则到期释放，不能把之后到期视作改期失败。测试数据仅用于本地回归。

单元覆盖整数分计算、时间/时区、区间、请求异常恢复等；真实 PostgreSQL 覆盖租户/场馆隔离、同单多时段、并发抢场、到期竞争、钱包不透支、本金赠送分摊、重复回调、改期补退、权限撤销、人工接管、消息派发去重和助手反馈。完整数量及最终日志见 [实施状态](implementation-status.md)。包含继承住房纯单测的通过数量不能视为同等数量网球业务用例。

## 待外部执行或客户验收

F8–F11 已补齐受控 Gateway、业务事件、支付端口、持久渠道恢复及人工协作目录；F18 增加合成可信流水对照。真实企微收退款同步、检查点/迟到补查和退款 adapter 仍需开发，真实微信支付适配器仍需结合选定产品开发和联调，见 [外部接入状态](external-integration-gaps.md)。这些不是仅补凭据的工作，也不阻止本地 PMS 人工验收。

- 提供真实微信渠道及可信身份映射、各租户商户号，联调真实付款/查单/退款。
- 后台模型服务由平台 UI 独立配置；本轮默认真实接线因自动审批拒绝未启用，需要完成对应接线后单独验证。外部 Runtime 在体外管理自己的模型，另行验证 Gateway、受控交易和接管；两者不混为一个外部配置。
- 核对导入场馆球场、营业与价目、全部有效预约和会员期初本金/赠送；现有数据均为合成演示。
- 客户按真实工作人员权限走一片/多片/多时段、代订、退改、课程维护、异常核对及手机操作验收。
- 拟沿用 GreenPMS 服务器，达成一致前不部署；部署阶段核对域名、独立服务/数据/凭据与回调。当前启动程序明确拒绝 production，历史住房部署入口不可使用。

## F12 成功响应中断后的恢复验收

2026-09-19，仅本机合成数据。临时代理固定转发到原本地服务，收到上游完整成功响应后仅截断一次 JSON 响应体；未改应用接口、权限或支付服务。演练后代理已关闭，正常演示仍为 127.0.0.1:4273。

- 17 元线上充值确认成功但响应体被截断，页面提示需要核对。关闭充值框并刷新整个页面后，“查询原操作”仍可见，点击按原回执打开唯一充值 fb5aedd1。数据库只有一条 topup.begin 回执、一笔 PENDING 充值及原第 1 代渠道操作，钱包流水为 0；未模拟付款成功，也未重复确认。
- 从订单 A（e4205cfa）给 HUMAN 会话发送合成消息后截断响应，再从订单 B（46e7c038）打开该会话并重试；两次请求 messageId、正文摘要、page/orderId 完全一致，数据库仅一条消息且外部派发为 0。
- 首次演练发现顶部仍显示当前订单 B、与下方原消息 A 提示不一致；已修复为 pending 优先显示原快照。新版 tennis-PlQNya-z.js 重复同场景，顶部、底部均显示待核实的 A，重试后正常恢复当前 B 提示；数据库再次确认唯一消息和零派发。

本次覆盖成功响应体丢失及页面刷新/换单恢复，不声称全部网络故障、真实微信、外部 Runtime 或客户人工验收通过。typecheck、69 文件/1,284 项单元及 Web build 通过；后端/迁移未改，F11 的 22 文件/276 项 PG 集成结果沿用，没有重复运行。详细证据见总项目 outputs/Tennis-PMS-F12-verification.log。

F12 同时核对当前桌面长页返回：工作台台账滚动高度 5,717、位置 4,817.5；切换到预订订单再返回，异步数据加载后高度和位置均与原值一致。只证明本次桌面实例，真实手机仍待人工验收。


## F16 租户期限配置浏览器核对（2026-09-19）

模拟格林租户在“场地与定价→预订期限”由5/10改为3/7，保存成功、刷新后表单显示3/7。为演示球友生成1号场2026-09-19 18:00–19:00、120元报价60ccca91，页面显示“确认后锁场待付款7分钟”。随后将配置恢复5/10，返回报价仍为7分钟；确认创建7525aa33，页面创建07:10、待付截止07:17。定点数据库核对报价约180秒、普通占位约420秒，当前策略5/10、revision3、付款记录0条。通过正常取消入口填写“F16本地合成期限验证完成”后，页面显示已取消、相关球场已释放，未产生付款/退款。

当前视口截图核对表单和原绿色/浅灰绿风格；未进行全设备验收。迁移证据及金额/期限证明已保存在总项目F16验证日志；以上为开发者验证，不代替客户签收。


## F17 响应式布局与导航检查（2026-09-19）

修复三个有浏览器证据的布局问题：顶部工具组不再被压缩到按钮宽度以下，空间不足时换行；排场日期栏允许换行，避免“今天”按钮超出窄屏；网球移动导航的断点从760px统一到继承样式的860px，消除761–860px之间侧栏和底栏同时隐藏的导航空档。仅调整网球CSS，保留GreenPMS配色、控件与触控大小，不改变交易、权限或数据库。

浏览器记录实际CSS尺寸与滚动宽度：约321px排场外层从338px溢出恢复到321px，约469px顶部从475px恢复到468px（整数测量取整）；排场表的横向滚动仍限制在自身容器。约775px原导航空档可正常进入订单；860px显示底栏，861px显示侧栏，约1281–1300px桌面排场/设置外层无横溢。窄屏检查资产、预订期限、订单详情及订单关联助手，助手消息输入可滚动到达，关闭后返回订单；桌面复核订单/助手打开返回。本轮测试输入已用键盘清空，没有发送消息、提交退改或产生资金操作。

主会话typecheck、71文件/1,360项单元及PR格式8项通过；最终CSS构建通过，资产`tennis-BBTaD38K.js` / `tennis-Bm2oNfl_.css`，浏览器已reload核对。没有后端变更，沿用F16的23文件/291项PostgreSQL结果，不宣称本轮重跑集成。原始日志及几何证据存于总项目`outputs/Tennis-PMS-F17-verification.log`与`Tennis-PMS-F17-browser-evidence.json`。

检查使用IAB浏览器尺寸覆盖；当时dpr约0.8，证据逐项记录innerWidth与visualViewport，不能把覆盖参数直接称为真机尺寸。截图存在工具裁切，因此仅报告已验证的DOM布局与操作，不声称完整视觉签收；真实触屏、中文输入法、手机软键盘及客户人工验收仍待完成。临时尺寸覆盖已恢复。真实微信/商户/外部Runtime与生产接入仍未完成，Goal不标完成。
